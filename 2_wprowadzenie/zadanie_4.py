#Napisz skrypt, który pobiera od użytkownika liczbę
#i wypisuje na ekran wartośc bezwzględną tej liczby

inserted_value = input("Insert value\n")
inserted_value = float(inserted_value)
absolut_number_value = (abs(inserted_value))
print("Absolut number value = " + str(absolut_number_value))